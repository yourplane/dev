# Base URL of the dev-server API. If unset, the app uses /api (same origin);
# the Vite dev server proxies /api to http://127.0.0.1:8000 (single-port dev).
# Set this to talk to the backend directly, e.g. http://localhost:8000
# VITE_DEV_SERVER_URL=http://localhost:8000
