# dev (CLI)

CLI for managing AI developer tasks with Cursor agent integration. Part of the dev workspace (see repository root). Install with `pip install -e .` from this directory or `pip install -e cli` from the repo root when using the workspace.
