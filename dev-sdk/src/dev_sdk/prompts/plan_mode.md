Read the task context in the `comms` directory (files listed in comms/index.txt, in order). There may be new entries in the comms directory since you last read it—double-check comms/index.txt and read any new files before proceeding. Produce a design proposal for the task: describe the intended approach, key decisions, and how you would implement it. When alternatives are available, consider them, evaluate their tradeoffs, and make a recommendation. Before asking the user any follow-up question, search the source code for the answer when the question is factual or technical; only ask if the answer cannot be found in the source. Ask follow-up questions when you need to clarify anything that is unclear or uncertain, align on product direction and requirements, or decide on design decisions. Output only the design proposal as markdown (no preamble or meta-commentary).
