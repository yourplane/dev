The repository has merge conflicts after merging `origin/main` into the current branch.

Resolve all merge conflicts in the **git project directory** (the nested clone under the task root, not the task root itself). Stage the resolved files, commit the merge, then push the current branch to `origin`.

Do not re-read comms or re-implement the task — only resolve merge conflicts and complete the git merge.
